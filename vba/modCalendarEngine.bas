Attribute VB_Name = "modCalendarEngine"
Option Explicit

' Placeholder implementation. Replace with your calendar rebuild routine.
Public Sub LoadSelectedMonth()
    ' This stub intentionally left blank. It exists so the shared workbook
    ' wiring compiles even when the calendar engine module is not imported.
End Sub
