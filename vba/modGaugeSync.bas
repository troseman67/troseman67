Attribute VB_Name = "modGaugeSync"
Option Explicit

' Placeholder that allows the workbook to compile without the downstream gauge
' synchronisation helpers. Replace with your implementation that propagates
' gauge log data back to the calendar grid as needed.
Public Sub RefreshGaugeGridFromLog()
End Sub
